rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isValidId(id) {
      return id is string && id.size() > 0 && id.size() <= 64;
    }

    match /cities/{cityId} {
      allow get, list: if true;
      allow write: if isValidId(cityId);
    }

    match /fares/{fareId} {
      allow get, list: if true;
      allow write: if isValidId(fareId);
    }

    match /vehicles/{vehicleId} {
      allow get, list: if true;
      allow write: if isValidId(vehicleId);
    }

    match /bookings/{bookingId} {
      allow get, list: if true;
      allow write: if isValidId(bookingId);
    }

    match /shipments/{shipmentId} {
      allow get, list: if true;
      allow write: if isValidId(shipmentId);
    }

    match /users/{userId} {
      allow get, list: if true;
      allow write: if isValidId(userId);
    }

    match /notifications/{notificationId} {
      allow get, list: if true;
      allow write: if isValidId(notificationId);
    }

    match /settings/{settingsId} {
      allow get, list: if true;
      allow write: if isValidId(settingsId);
    }

    match /test/{testId} {
      allow read, write: if true;
    }
  }
}

